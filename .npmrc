strict-peer-dependencies=false
auto-install-peers=true
# Published packages carry a provenance attestation, so consumers can verify
# they were built by the release workflow from this repository.
provenance=true
